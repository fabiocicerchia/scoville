name: ci

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          # Dev dependencies come from a hash-pinned lockfile; the package
          # itself installs with --no-deps so nothing unpinned sneaks in.
          # Regenerate after touching pyproject.toml:
          #   uv pip compile pyproject.toml --extra dev --generate-hashes \
          #     --universal -p 3.10 -o requirements-dev.txt
          pip install --require-hashes -r requirements-dev.txt
          pip install -e . --no-deps

      - name: Pytest
        run: pytest -q
